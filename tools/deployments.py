"""Simulated deployment history tool — returns recent deploys for a service."""

# TODO: Implement get_deployments(service, time_range) -> list of deployments
# - Load simulated deployment data from simulation/data/
# - Each deployment: id, service, timestamp, author, description, commit_sha, status
# - Support filtering by service and time range
